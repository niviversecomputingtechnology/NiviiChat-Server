NODE_ENV=development

# Postgres (matches docker-compose service "db")
POSTGRES_USER=nivichat
POSTGRES_PASSWORD=change_me
POSTGRES_DB=nivichat
DATABASE_URL=postgresql://nivichat:change_me@db:5432/nivichat?schema=public

# Auth
JWT_ACCESS_SECRET=change_me
JWT_REFRESH_SECRET=change_me
JWT_ACCESS_EXPIRES_IN=5m
JWT_REFRESH_EXPIRES_IN=30d

# App
APP_VERSION=2.0.11
API_PORT=3000
HOST=0.0.0.0
WS_PORT=8080
LOG_LEVEL=info
# Comma-separated allowlist of client origins; unset allows all (dev default)
CORS_ORIGIN=

# Internal REST -> WS broadcast (same process pair, not client-facing)
WS_INTERNAL_URL=http://ws:8080
WS_INTERNAL_SECRET=change_me

# Push (referenced by client's ONESIGNAL_ID — keep out of source, inject here)
ONESIGNAL_APP_ID=
ONESIGNAL_REST_API_KEY=
